# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Secrets — real keys are injected via --dart-define, never committed.
# The lib/config/*.dart files contain only placeholders and ARE committed.
# Local dev secrets live in run_dev.ps1 (below) and .env (never committed).
run_dev.ps1
.env
.env.local

# Local tooling scripts that may contain pasted API keys (TTS/translation)
audio_scripts/

# Downloaded audio files — large binaries, hosted on Cloudflare R2
gita_audio/

# Flutter temp logs
C:/Users/user/AppData/Local/Temp/fr*.log

# AI-generated Gita commentary cache (tools/import_gita.mjs)
tools/.gita_commentary_cache.json

# AI-assisted Upanishad enrichment cache (tools/import_upanishads.mjs)
tools/.upanishad_cache.json

# Wrangler local build artifacts
cloudflare_worker/.wrangler/
